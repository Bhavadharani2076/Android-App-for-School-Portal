plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.simats.schoolportal"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.simats.schoolportal"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }
    signingConfigs {
        release {
            storeFile file("D:/schoolproject/schoolportal/my-release-key.jks")  // Correct path to your .jks file
            storePassword "Gnanasaraswathy@12"  // Replace with your actual keystore password
            keyAlias "my-key-alias"               // Replace with the key alias you provided
            keyPassword "Gnanasaraswathy@12"         // Replace with your actual key password
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release // Make sure this is correct
            minifyEnabled true // You can set this to false for testing if needed
            shrinkResources true // You can disable if you face issues
        }
    }
}

flutter {
    source = "../.."
}
